# 视觉处理学习方案

> 深度学习视觉 + 视觉多模态,应用方向偏向三维视觉(SLAM / 3D 重建 / 深度估计 / 多视角几何)
> 路线:从小白到专家 · 深度 + 广度 · 全程 AI 执行命令并解释,你只提问/验收
> 版本 v1 · 2026-08-16 · **当前仅方案,不执行**

---

## 0. 方案总览

### 0.1 定位

这是一条「深度学习计算机视觉 + 多模态」的学习路线,不是传统 OpenCV 图像处理路线。
你已跳过经典 CV 部分(滤波/边缘/角点那套),直接以 PyTorch 为工具,把图像当作张量,
从神经网络分类一路走到三维重建、视觉 SLAM,最后接上视觉语言模型(VLM)和扩散模型。

但因为应用方向是三维视觉,几何那一块(相机模型、多视角几何、刚体变换)必须补,
这是三维视觉的地基,和神经网络同等重要。

### 0.2 算力分工

- **本机 WSL2(CPU)** 承担:环境搭建、所有数据预处理、NumPy/张量练习、小模型(MLP、LeNet、小 CNN)
  训练与推理、相机几何与对极几何练习、ORB 特征、立体匹配的 CPU 部分、CLIP/小 VLM 推理。
- **AutoDL(GPU)** 承担:CNN/ResNet 训练、检测(YOLO/Faster R-CNN)、分割(U-Net/Mask R-CNN)、
  单目深度(MiDaS/DPT)、NeRF、3D Gaussian Splatting、扩散模型推理、大 VLM 推理。

分工原则:**CPU 跑通逻辑,GPU 跑规模。** 能先在本机把代码写对、跑 1 个 batch 不出错,
再上传 AutoDL 跑完整训练,省卡钱。

### 0.3 网络与镜像约定(国内)

- pip 走阿里云:`pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/`
- PyTorch 走阿里云 / 官方 cu121 源(本机 CPU 用阿里云,云端 CUDA 用官方源)
- GitHub 走 ghfast:`git clone https://ghfast.top/https://github.com/...`
- 数据集与预训练权重走 ModelScope(`modelscope.cn`),避开 torchvision/hub 的境外下载
- 大文件断点续传:rsync / aria2

### 0.4 交付约定

- 每课:目标 → 核心概念 → 命令(逐条带解释)→ 预期结果
- 全程 AI 执行命令并解释,你只负责提问、验收、控制台安全操作
- 讲解不用表格,一律列表/代码块
- 每课结束有「验收点」,达标才进下一课

### 0.5 前置要求

- Python 已熟练(你的 Blender 基础)
- 会一点 NumPy 即可(阶段 0 会带)
- 线性代数(矩阵乘法、转置)会基础即可,阶段 4 会系统补
- 不需要提前懂微积分,反向传播那课会带

---

## 1. 阶段总览(8 阶段 · 34 课)

- **阶段 0 · 环境装备**(3 课)—— WSL2/AutoDL 环境、镜像、张量入门
- **阶段 1 · 图像与张量的语言**(3 课)—— 图像读写、颜色空间、张量运算、autograd、DataLoader
- **阶段 2 · 神经网络与图像分类**(5 课)—— MLP、反向传播、CNN、经典网络、迁移学习
- **阶段 3 · 目标检测与实例分割**(5 课)—— IoU/mAP、Faster R-CNN、YOLO、U-Net、Mask R-CNN
- **阶段 4 · 三维视觉的几何根基**(5 课)—— 相机模型、刚体变换、对极几何、三角化/PnP、立体视觉
- **阶段 5 · 深度估计与三维重建**(5 课)—— 单目深度、立体匹配、SfM(COLMAP)、NeRF、3DGS
- **阶段 6 · 视觉 SLAM**(4 课)—— ORB 特征、视觉里程计、ORB-SLAM、回环检测
- **阶段 7 · 视觉多模态**(4 课)—— CLIP 对比学习、VLM 推理、扩散模型、文本/图像到 3D

**毕业项目**:三维重建 + 语义理解融合的端到端 demo(阶段 5~7 穿插,最终章给出)。

---

## 2. 阶段 0 · 环境装备(3 课)

### 课 0.1 本机 WSL2 环境

**目标**:建好隔离的 venv,装上 CPU 版 PyTorch 与 CV 全家桶。

**命令(逐条解释)**:

```bash
# 1) 确认 Python 版本 ≥ 3.10
python3 --version

# 2) 建隔离虚拟环境(不污染系统,以后所有课都在这个环境里跑)
python3 -m venv ~/vision

# 3) 激活(以后每次开新终端先执行这条)
source ~/vision/bin/activate

# 4) pip 全局指向阿里云镜像,以后装包不再被墙
pip config set global.index-url https://mirrors.aliyun.com/pypi/simple/

# 5) 升级 pip
pip install -U pip

# 6) 装 CPU 版 PyTorch + 视觉全家桶
#    Linux 上从 PyPI/阿里云默认拉到的 torch 就是 CPU 版
pip install torch torchvision numpy opencv-python-headless matplotlib pillow tqdm
```

**预期结果**:

```bash
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
# 输出类似:2.4.0+cpu False
# cuda.is_available() 为 False 是正常的——本机就是 CPU 路线
```

**验收点**:`import torch / torchvision / cv2 / numpy` 全部不报错。

---

### 课 0.2 AutoDL GPU 环境与数据同步

**目标**:会用 autodl CLI 租卡、传代码、把结果拉回来。你已有 token
(`~/.config/autodl_cli/config.toml` + `AUTODL_TOKEN`),这课只补「怎么用」。

**命令(逐条解释)**:

```bash
# 1) 查看 autodl 子命令(具体参数以 --help 为准,执行时再核对)
autodl --help

# 2) 租一台卡(镜像选 PyTorch 官方镜像,自带 CUDA + torch,不必重装)
#    具体命令执行时按 autodl CLI 实际语法填

# 3) 上传本地代码/小数据到云端实例(替换 host/port/key)
scp -P <端口> -i <密钥> ./train.py root@<实例地址>:~/

# 4) 拉回训练结果 / 日志 / 模型权重
scp -P <端口> -i <密钥> root@<实例地址>:~/runs/exp/*.pth ./local/

# 5) 目录级同步用 rsync(断点续传,大模型权重推荐)
rsync -avzP -e "ssh -p <端口> -i <密钥>" root@<实例地址>:~/runs/ ./runs/
```

**云端装包约定**(在 AutoDL 实例内执行,注意是 CUDA 源):

```bash
# AutoDL 官方镜像一般已带 torch(CUDA);若需重装:
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
pip install ultralytics timm open_clip_torch diffusers
```

**预期结果**:`nvidia-smi` 显示显存;`python -c "import torch; print(torch.cuda.is_available())"` 输出 `True`。

**验收点**:能从本机 scp 一个文件上去、再拉回来,闭环打通。

---

### 课 0.3 张量与 NumPy 入门

**目标**:建立「一切图像都是张量」的思维,这是整条路线的地基。

**代码(逐段解释)**:

```python
import numpy as np
import torch

# NumPy:多维数组是 CV 的通用语言
a = np.arange(12).reshape(3, 4)      # 3行4列
print(a.shape, a.dtype)              # (3, 4) int64

# torch.Tensor:和 NumPy 几乎同构,但能上 GPU、能求梯度
t = torch.from_numpy(a)              # 从 numpy 转换
print(t.shape, t.dtype, t.device)    # torch.Size([3, 4]) torch.int64 cpu

# 关键:图像是 (C, H, W) 或 (H, W, C) 的三维张量,批处理加一维成 (N, C, H, W)
img_like = torch.zeros(2, 3, 224, 224)   # 2张 3通道 224x224 图
print(img_like.shape)
```

**预期结果**:

```
(3, 4) int64
torch.Size([3, 4]) torch.int64 cpu
torch.Size([2, 3, 224, 224])
```

**验收点**:能说出 `(N, C, H, W)` 每个字母代表什么,以及 PyTorch 与 NumPy 的差异(设备、梯度)。

---

## 3. 阶段 1 · 图像与张量的语言(3 课)

### 课 1.1 图像读写与颜色空间

**目标**:用 OpenCV/PIL 读写图像,搞清 BGR/RGB、灰度、归一化。

**代码(逐段解释)**:

```python
import cv2
import numpy as np

img = cv2.imread("cat.jpg")           # 默认读成 BGR,这是 OpenCV 的老坑
print(img.shape)                      # (H, W, 3)

img_rgb = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)  # 转 RGB
gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)    # 转灰度
print(gray.shape)                     # (H, W)

# 归一化:深度学习输入通常要 [0,1] 或均值方差标准化
x = img_rgb.astype(np.float32) / 255.0
print(x.min(), x.max())               # 0.0 1.0
```

**预期结果**:打印 `(H, W, 3)`、`(H, W)`、`0.0 1.0`。

**验收点**:能解释 BGR 与 RGB 差在哪、为什么深度学习通常要归一化。

---

### 课 1.2 张量运算、广播与 autograd

**目标**:掌握张量四则、广播、以及 PyTorch 自动求导(这是训练一切的根基)。

**代码(逐段解释)**:

```python
import torch

# 广播:形状不同也能算,低维会自动扩展
a = torch.ones(3, 4)
b = torch.ones(4)
print((a + b).shape)   # (3, 4),b 沿最后一维广播

# autograd:追踪计算图,自动算梯度
x = torch.tensor(2.0, requires_grad=True)
y = x ** 3              # y = x^3
y.backward()            # 反向传播
print(x.grad)           # dy/dx = 3*x^2 = 12.0
```

**预期结果**:

```
torch.Size([3, 4])
tensor(12.)
```

**验收点**:能手算一个简单函数的梯度,并说清 `.backward()` 在做什么。

---

### 课 1.3 数据集与 DataLoader

**目标**:会用 `Dataset`/`DataLoader` 组织数据,这是所有训练的第一行代码。

**代码(逐段解释)**:

```python
from torchvision import datasets, transforms
from torch.utils.data import DataLoader

# 变换:ToTensor 把 PIL 图转成 (C,H,W) 且归一化到 [0,1]
transform = transforms.Compose([
    transforms.ToTensor(),
])

# CIFAR-10:32x32 十类小图,适合 CPU 上跑通逻辑
ds = datasets.CIFAR10(root="./data", train=True, download=True, transform=transform)
dl = DataLoader(ds, batch_size=64, shuffle=True)

x, y = next(iter(dl))   # 取一个 batch
print(x.shape, y.shape) # (64, 3, 32, 32) (64,)
```

**预期结果**:

```
Files already downloaded and verified
(64, 3, 32, 32) (64,)
```

> 注:CIFAR 下载若慢,改走 ModelScope 数据集;执行时再处理,当前仅方案。

**验收点**:能自己写一个 `Dataset` 子类加载自定义图片文件夹。

---

## 4. 阶段 2 · 神经网络与图像分类(5 课)

> 本阶段在本机 CPU 就能跑(小模型),真正的大 ResNet 训练放阶段 2.4~2.5 上 AutoDL。

### 课 2.1 从线性回归到 MLP

**目标**:手写一个多层感知机,理解「线性层 → 激活 → 前向」。

**核心概念**:线性层 `y = Wx + b`;激活函数(ReLU)引入非线性;损失函数(交叉熵)衡量差距。

**代码(手写前向,不依赖现成 nn 也能跑通)**:

```python
import torch, torch.nn as nn

class MLP(nn.Module):
    def __init__(self):
        super().__init__()
        self.net = nn.Sequential(
            nn.Flatten(),          # 32x32x3 -> 3072
            nn.Linear(3072, 256),
            nn.ReLU(),
            nn.Linear(256, 10),    # 10 类
        )
    def forward(self, x):
        return self.net(x)

model = MLP()
x = torch.randn(2, 3, 32, 32)
print(model(x).shape)   # (2, 10),每行是 10 类的 logits
```

**预期结果**:`torch.Size([2, 10])`。

**验收点**:能说出每个 `Linear` 的输入输出维度怎么算出来的。

---

### 课 2.2 反向传播、优化器与训练循环

**目标**:写出一个完整训练循环(前向 → 算 loss → 反向 → 更新),这是全部训练的模板。

**代码(完整可跑,CPU 上训练 MNIST/小数据集)**:

```python
import torch, torch.nn as nn, torch.optim as optim
from torch.utils.data import DataLoader
from torchvision import datasets, transforms

device = "cuda" if torch.cuda.is_available() else "cpu"

# 数据
transform = transforms.Compose([transforms.ToTensor()])
dl = DataLoader(datasets.MNIST("./data", train=True, download=True, transform=transform),
                batch_size=64, shuffle=True)

# 模型 + 损失 + 优化器
model = nn.Sequential(nn.Flatten(), nn.Linear(784, 128), nn.ReLU(), nn.Linear(128, 10)).to(device)
loss_fn = nn.CrossEntropyLoss()
opt = optim.SGD(model.parameters(), lr=0.01)

# 训练循环(模板,记住这 5 步)
for epoch in range(3):
    for x, y in dl:
        x, y = x.to(device), y.to(device)
        opt.zero_grad()          # 1) 清零梯度
        out = model(x)           # 2) 前向
        loss = loss_fn(out, y)   # 3) 算损失
        loss.backward()          # 4) 反向传播
        opt.step()               # 5) 更新参数
    print(f"epoch {epoch} loss {loss.item():.4f}")
```

**预期结果**:loss 从约 2.3 一路降到 0.3 以下(CPU 也能几分钟跑完 3 个 epoch)。

**验收点**:能画出这 5 步的流程图,并说出 `zero_grad()` 为什么不能省。

---

### 课 2.3 CNN(卷积 + 池化)在 CIFAR-10

**目标**:理解卷积核、感受野、池化,手写一个 CNN 并训练。

**核心概念**:卷积 = 局部滑动窗口加权;池化 = 降采样保特征;CNN 参数远少于 MLP。

**代码(关键层)**:

```python
import torch.nn as nn

class CNN(nn.Module):
    def __init__(self):
        super().__init__()
        self.features = nn.Sequential(
            nn.Conv2d(3, 32, 3, padding=1), nn.ReLU(),
            nn.MaxPool2d(2),
            nn.Conv2d(32, 64, 3, padding=1), nn.ReLU(),
            nn.MaxPool2d(2),
        )
        self.classifier = nn.Sequential(
            nn.Flatten(),
            nn.Linear(64 * 8 * 8, 10),
        )
    def forward(self, x):
        return self.classifier(self.features(x))

model = CNN()
print(model(torch.randn(2, 3, 32, 32)).shape)  # (2, 10)
```

**预期结果**:训练 10 个 epoch 后 CIFAR-10 测试集准确率约 **65%~75%**(CPU 慢,可先少跑几轮)。

**验收点**:能算出一个卷积层的输出尺寸公式,能解释感受野是什么。

---

### 课 2.4 经典网络(LeNet → AlexNet → VGG → ResNet)

**目标**:读懂四个里程碑网络,重点是 ResNet 的残差连接。

**核心概念**:越深越难训练;残差连接 `out = F(x) + x` 让梯度能直通,解决了退化。

**代码(加载预训练)**:

```python
import torchvision.models as m

resnet18 = m.resnet18(pretrained=True)  # 或 weights=... 新写法
print(resnet18)  # 观察 layer1~4 和残差块结构

# 改最后一层做 10 类分类
resnet18.fc = nn.Linear(512, 10)
```

**预期结果**:能看到 ResNet 有 4 个 stage,每层都是残差块。

> 预训练权重若从 download.pytorch.org 下载慢,改走 ModelScope 的权重镜像,执行时处理。

**验收点**:能用一段话讲清 ResNet 为什么能堆到 50/101 层还训得动。

---

### 课 2.5 迁移学习与数据增强(上 AutoDL)

**目标**:冻结骨干、只训分类头,或微调全网络,掌握数据增强。

**核心概念**:迁移学习 = 拿 ImageNet 学到的通用特征,小数据也能训出好模型;增强 = 翻转/裁剪/颜色抖动扩充数据。

**命令(上传 AutoDL 跑)**:

```bash
# 本机写好 train_finetune.py,scp 上云,在 AutoDL 里执行:
python train_finetune.py --epochs 15 --batch-size 128 --lr 1e-3
```

**关键代码**(增强 + 冻结骨干):

```python
transform = transforms.Compose([
    transforms.RandomHorizontalFlip(),
    transforms.RandomCrop(32, padding=4),
    transforms.ColorJitter(0.2, 0.2, 0.2),
    transforms.ToTensor(),
])

model = m.resnet18(pretrained=True)
for p in model.parameters():   # 冻结全部参数
    p.requires_grad = False
model.fc = nn.Linear(512, 10)  # 只训这层
```

**预期结果**:冻结骨干训 10 个 epoch,CIFAR-10 上快速到 **85%+**;全微调能到 **90%+**。

**验收点**:能解释「冻结骨干」为什么省时又能出效果。

---

## 5. 阶段 3 · 目标检测与实例分割(5 课)

> 检测/分割训练吃显存,全部在 AutoDL 上做;本机只跑推理 demo。

### 课 3.1 检测任务与评价指标(IoU / mAP)

**目标**:搞清检测的定位 + 分类双重任务,以及怎么打分。

**核心概念**:
- IoU(交并比)= 两个框的交集面积 / 并集面积
- mAP = 不同 IoU 阈值下「精确率-召回率」曲线下面积的平均

**代码(手写 IoU)**:

```python
def iou(a, b):
    # a, b 是 [x1, y1, x2, y2]
    inter = (min(a[2], b[2]) - max(a[0], b[0])) * (min(a[3], b[3]) - max(a[1], b[1]))
    inter = max(0, inter)
    area = lambda x: (x[2]-x[0]) * (x[3]-x[1])
    return inter / (area(a) + area(b) - inter)

print(iou([0,0,2,2], [1,1,3,3]))   # 期望约 1/7 ≈ 0.1429
```

**预期结果**:`0.14285714285714285`。

**验收点**:能说出为什么用 IoU 而不是只看中心点距离。

---

### 课 3.2 两阶段检测 Faster R-CNN

**目标**:理解「先提候选框(RPN),再分类回归」的两阶段范式。

**代码(torchvision 内置,直接推理)**:

```python
import torchvision
from torchvision.models.detection import fasterrcnn_resnet50_fpn
from torchvision.models.detection.faster_rcnn import FastRCNNPredictor

model = fasterrcnn_resnet50_fpn(pretrained=True)
model.eval()
# 推理:输入 [C,H,W] 的 tensor list
```

**预期结果**:对一张街景图输出若干框 + 类别 + 置信度。

**验收点**:能讲清 RPN 和 Fast R-CNN 头各自负责什么。

---

### 课 3.3 单阶段检测 YOLO

**目标**:会用 ultralytics 的 YOLO 做训练与推理,理解「一次前向直接出框」的锚框/网格思路。

**命令**:

```bash
# AutoDL 上
pip install ultralytics

# 训练(用 coco128 小数据集练手,先 3 个 epoch 验证管线)
yolo detect train data=coco128.yaml model=yolov8n.pt epochs=3 imgsz=640

# 推理一张图
yolo detect predict model=runs/detect/train/weights/best.pt source=bus.jpg
```

**预期结果**:`runs/detect/train/` 下产出权重与指标图(mAP50 会随 epoch 上升);`predict` 在图上画出检测框。

**验收点**:能说出 YOLO 和 Faster R-CNN 的核心区别(端到端 vs 两阶段,速度 vs 精度)。

---

### 课 3.4 语义分割 U-Net

**目标**:手写 U-Net 的编码器-解码器 + 跳跃连接,理解逐像素分类。

**核心概念**:下采样提特征,上采样恢复分辨率;跳跃连接把浅层细节拼回来。

**代码(U-Net 骨架,示意)**:

```python
class UNet(nn.Module):
    def __init__(self):
        super().__init__()
        # 编码:Conv -> ReLU -> MaxPool 重复 4 次
        # 解码:上采样 + 拼接 + Conv 重复 4 次
        # 输出:1x1 卷积得到每像素类别分数
        ...
    def forward(self, x):
        # 返回 (N, num_classes, H, W)
        ...
```

**预期结果**:在小数据集(如 Carvana 分割)上训练,输出与原图同尺寸的类别 mask。

**验收点**:能解释为什么语义分割输出是 `(N, C, H, W)` 而非 `(N, C)`。

---

### 课 3.5 实例分割 Mask R-CNN

**目标**:在检测框基础上再加一个 mask 分支,区分「同一类的不同个体」。

**代码(torchvision 内置)**:

```python
from torchvision.models.detection import maskrcnn_resnet50_fpn
model = maskrcnn_resnet50_fpn(pretrained=True)
model.eval()
```

**预期结果**:对图片输出每个实例的框 + mask + 类别(COCO 80 类)。

**验收点**:能说清「语义分割」和「实例分割」的区别(像素级类别 vs 个体级像素)。

---

## 6. 阶段 4 · 三维视觉的几何根基(5 课)

> 这是三维方向的地基。全部用 NumPy/OpenCV,本机 CPU 就能做,不碰深度学习。

### 课 4.1 相机模型(针孔相机与内参)

**目标**:搞清「世界 3D 点 → 像素 2D 点」的投影过程。

**核心概念**:
- 内参矩阵 K:`[[fx, 0, cx], [0, fy, cy], [0, 0, 1]]`
- 投影:`s * [u, v, 1]^T = K * [X, Y, Z]^T`

**代码**:

```python
import numpy as np
K = np.array([[525., 0, 320.], [0, 525., 240.], [0, 0, 1]])  # 示例内参
P = np.array([1.0, 2.0, 5.0])          # 一个 3D 点
uv = K @ P
u, v = uv[0]/uv[2], uv[1]/uv[2]         # 归一化得到像素坐标
print(u, v)
```

**预期结果**:`(425.0, 450.0)`,即该 3D 点投影到图像 (425, 450)。

**验收点**:能解释 fx/fy(焦距·像素)、cx/cy(主点)的物理含义。

---

### 课 4.2 刚体变换与坐标系(旋转矩阵/四元数/齐次坐标)

**目标**:掌握 3D 旋转的三种表达,以及齐次坐标下的变换矩阵。

**核心概念**:
- 旋转矩阵 R(3x3 正交)、四元数 q(4 维,避免万向锁)、齐次变换 `[R|t]`
- `P_cam = R @ P_world + t`

**代码**:

```python
import numpy as np
from scipy.spatial.transform import Rotation as R

r = R.from_euler('xyz', [0, 0, 90], degrees=True)   # 绕 z 转 90°
Rmat = r.as_matrix()
q = r.as_quat()                       # 转四元数
t = np.array([1., 0., 0.])            # 平移
T = np.eye(4); T[:3,:3] = Rmat; T[:3,3] = t   # 4x4 齐次变换

P = np.array([1., 0., 0., 1.])        # 齐次点
print(T @ P)                          # 旋转+平移后的坐标
```

**预期结果**:绕 z 转 90° 把 (1,0,0) 转到 (0,1,0),再平移得 `(1,1,0,1)`。

**验收点**:能说出四元数相对旋转矩阵/欧拉角的优势。

---

### 课 4.3 多视角几何(对极几何、本质矩阵、基础矩阵)

**目标**:理解两张图之间的几何约束——同一 3D 点在两图的投影满足对极约束。

**核心概念**:
- 本质矩阵 E(标定相机间):`x2^T E x1 = 0`
- 基础矩阵 F(像素坐标间):`x2^T F x1 = 0`
- 对极线、极点、基线

**代码(OpenCV 算基础矩阵)**:

```python
import cv2, numpy as np
# pts1, pts2:两图匹配点(N x 2)
F, mask = cv2.findFundamentalMat(pts1, pts2, cv2.FM_RANSAC)
print(F.shape)   # (3, 3)
```

**预期结果**:得到 3x3 的 F,能验证 `x2^T F x1 ≈ 0`。

**验收点**:能画出对极几何图(两相机中心、基线、对极线)并标出极点。

---

### 课 4.4 三角化与 PnP

**目标**:从两视图恢复 3D 点(三角化),以及从 3D-2D 对应求相机位姿(PnP)。

**核心概念**:
- 三角化:两条投影射线相交,还原 3D 坐标
- PnP:已知若干 3D 点及其 2D 投影,反求 R、t

**代码**:

```python
import cv2
# 三角化
P1, P2 = np.eye(3,4), np.hstack([R, t])   # 两相机投影矩阵
X_hom = cv2.triangulatePoints(P1, P2, pts1.T, pts2.T)
X = X_hom[:3] / X_hom[3]                  # 齐次转非齐次 -> 3D 点

# PnP
ret, rvec, tvec = cv2.solvePnP(obj3d, img2d, K, dist)
```

**预期结果**:三角化恢复的 3D 点与真值接近;PnP 得到旋转向量/平移向量。

**验收点**:能解释三角化和 PnP 是「互为逆问题」。

---

### 课 4.5 立体视觉(视差与深度)

**目标**:理解双目相机怎么靠视差算深度。

**核心概念**:深度 `Z = f * B / d`,其中 B 是基线,d 是视差(两图对应点的水平位移)。

**代码**:

```python
import cv2
stereo = cv2.StereoSGBM_create(numDisparities=64, blockSize=9)
disp = stereo.compute(imgL_gray, imgR_gray)   # 视差图
# 深度 Z = f * B / disp(需按实际基线换算)
```

**预期结果**:得到视差图,近处物体视差大(更亮),远处小(更暗)。

**验收点**:能写出 `Z = fB/d` 并解释每个符号。

---

## 7. 阶段 5 · 深度估计与三维重建(5 课)

> 本阶段开始需要 GPU,AutoDL 为主。这是「三维方向」的产出阶段。

### 课 5.1 单目深度估计(MiDaS / DPT)

**目标**:用预训练模型从单张图直接预测深度图。

**核心概念**:单目深度是「有歧义」的任务,靠大数据先验;MiDaS 是通用深度估计标杆。

**代码(本机 CPU 可跑 MiDaS small,大模型上 AutoDL)**:

```python
import torch
# 权重从 ModelScope 拉(避免 torch.hub 境外下载),执行时处理
model = torch.hub.load("intel-isl/MiDaS", "MiDaS_small")
model.eval()
# 输入预处理 -> 输出 (1, H, W) 逆深度,再反变换成深度
```

**预期结果**:输入一张街景,输出灰度深度图(近亮远暗)。

**验收点**:能解释「单目深度为什么需要先验/大模型」。

---

### 课 5.2 立体匹配实战

**目标**:在真实双目图上跑通完整的视差→深度流程。

**命令/代码**:

```bash
# 用 KITTI 或 Middlebury 双目测试图(小样,下载快)
python stereo_depth.py --left L.png --right R.png --fx 721 --baseline 0.54
```

**预期结果**:输出逐像素深度图,与实际场景尺度一致。

**验收点**:能把视差图可视化并换算成物理深度。

---

### 课 5.3 运动恢复结构 SfM(COLMAP)

**目标**:从一堆无序照片恢复稀疏 3D 点云 + 相机位姿。

**命令**:

```bash
# 本机(WSL2)装 COLMAP
sudo apt install colmap

# 对一组图跑自动重建
colmap automatic_reconstructor --workspace_path ./ws --image_path ./images
```

**预期结果**:`ws/` 下生成稀疏重建结果(`sparse/`),可导出为点云 `.ply`,用 MeshLab 查看。

**验收点**:能说出 SfM 的流程:特征提取 → 匹配 → 增量式求位姿 → 三角化 → BA 优化。

---

### 课 5.4 神经辐射场 NeRF

**目标**:理解「用 MLP 隐式表达场景,体渲染合成新视角」。

**核心概念**:每个 3D 点 → (颜色, 密度);沿光线积分得像素颜色;光度损失训练。

**命令(AutoDL GPU,用 nerfstudio 或轻量实现)**:

```bash
git clone https://ghfast.top/https://github.com/nerfstudio-project/nerfstudio.git
cd nerfstudio && pip install -e .
# 对一组小图训练(如 lego 场景)
ns-train nerfacto --data data/nerfstudio/lego
```

**预期结果**:训练后能渲染出任意新视角的图片。

**验收点**:能画出「光线采样 → 体渲染 → 损失反传」的流程图。

---

### 课 5.5 3D Gaussian Splatting(3DGS)

**目标**:掌握当前主流的实时新视角合成方法。

**核心概念**:用一组 3D 高斯球表达场景,光栅化投影渲染,训练+渲染都比 NeRF 快很多。

**命令(AutoDL GPU)**:

```bash
git clone https://ghfast.top/https://github.com/graphdeco-inria/gaussian-splatting.git
# 或直接用 nerfstudio 的 splatfacto:
ns-train splatfacto --data data/nerfstudio/lego
```

**预期结果**:得到可实时交互旋转视角的场景,输出 `.ply` 高斯点云。

**验收点**:能说出 3DGS 相对 NeRF 快在哪(显式表达 + 光栅化)。

---

## 8. 阶段 6 · 视觉 SLAM(4 课)

> 把阶段 4 的几何 + 阶段 5 的重建串起来,做「边走边定位边建图」。CPU 可跑,GPU 加速。

### 课 6.1 ORB 特征与描述子

**目标**:理解怎么在图上找到「稳定、可重复」的角点并描述它们。

**代码**:

```python
import cv2
orb = cv2.ORB_create(nfeatures=2000)
kp, des = orb.detectAndCompute(gray, None)
print(len(kp), des.shape)   # (关键点数量, 32)
```

**预期结果**:打印关键点数量,画出来能看到特征点集中在角点/纹理处。

**验收点**:能解释为什么 SLAM 用 ORB(快 + 旋转不变 + 二值描述子)。

---

### 课 6.2 视觉里程计(最小实现)

**目标**:手写一个「相邻两帧求位姿」的 VO:匹配 → 本质矩阵 → 分解 → 三角化。

**核心概念**:VO 只算相邻帧的相对运动,不做全局优化,会漂移。

**代码(流程)**:

```python
# 1) ORB 匹配两帧 -> pts1, pts2
# 2) E, _ = cv2.findEssentialMat(pts1, pts2, K, RANSAC)
# 3) _, R, t, _ = cv2.recoverPose(E, pts1, pts2, K)
# 4) 累积得到轨迹
```

**预期结果**:在 KITTI 序列上输出一条相机轨迹,短距离内与真值接近。

**验收点**:能解释「视觉里程计为什么会有累计漂移」。

---

### 课 6.3 ORB-SLAM2/3 运行

**目标**:跑通完整 SLAM 系统(跟踪 + 局部建图 + 回环 + BA)。

**命令**:

```bash
git clone https://ghfast.top/https://github.com/UZ-SLAMLab/ORB_SLAM3.git
# 依赖:Pangolin、OpenCV、Eigen(按官方 README 编译)
./Examples/Monocular/mono_kitti Vocabulary/ORBvoc.txt Examples/Monocular/KITTI03.yaml path/to/kitti/03
```

**预期结果**:实时显示相机轨迹 + 稀疏地图 + 回环修正(轨迹闭合)。

**验收点**:能画出 ORB-SLAM 的三个并行线程(跟踪/局部建图/回环检测)。

---

### 课 6.4 回环检测与全局优化

**目标**:理解词袋模型(DBoW2)怎么认出「以前来过这」,以及位姿图优化怎么消除漂移。

**核心概念**:把图像描述子量化成「视觉单词」,用词袋向量快速检索历史帧。

**预期结果**:理解回环发生时,轨迹会「跳」回闭合,漂移被消除。

**验收点**:能解释为什么回环检测能治 VO 的累计漂移。

---

## 9. 阶段 7 · 视觉多模态(4 课)

> 接上大模型时代。本机 CPU 可跑 CLIP 与小 VLM 推理,扩散模型与大 VLM 上 AutoDL。
> 约定:不使用 qwen-vl / qwen-image(你的既定约束),图像生成走 ComfyUI/AutoDL。

### 课 7.1 CLIP 与对比学习

**目标**:理解「图文共享一个向量空间」,这是所有 VLM 的地基。

**核心概念**:对比学习——让匹配的图文对拉近、不匹配的推远。

**代码**:

```bash
pip install open_clip_torch
```

```python
import torch, open_clip
model, _, preprocess = open_clip.create_model_and_transforms("ViT-B-32", pretrained="laion2b_s34b_b79k")
# 对图片和文本分别编码,算余弦相似度,做零样本分类
```

**预期结果**:一张猫图与文本「a photo of a cat」的相似度最高,实现零样本分类。

**验收点**:能画出对比学习的「正样本拉近、负样本推远」示意。

---

### 课 7.2 VLM 视觉语言模型推理

**目标**:用开源 VLM 做看图问答/看图描述。

> 注意:避开 qwen-vl 系列,改用 BLIP2 / LLaVA / Florence-2 等。

**代码**:

```python
from transformers import Blip2Processor, Blip2ForConditionalGeneration
proc = Blip2Processor.from_pretrained("Salesforce/blip2-opt-2.7b")
model = Blip2ForConditionalGeneration.from_pretrained("Salesforce/blip2-opt-2.7b")
# 输入图片 + 问题,输出文本回答
```

**预期结果**:输入「图里有几只猫?」返回文本答案。

**验收点**:能说出 VLM 的典型结构:视觉编码器 + 桥接层 + 语言模型。

---

### 课 7.3 扩散模型(Stable Diffusion)原理与推理

**目标**:理解「正向加噪 → 反向去噪」的生成范式。

**核心概念**:DDPM 前向逐步加高斯噪声;U-Net 学会逐步去噪;文本用 CLIP 编码做条件。

**命令(AutoDL,或本机小步数 CPU 演示)**:

```bash
pip install diffusers
python sd_generate.py --prompt "a red cube on a table" --steps 30
```

**预期结果**:按提示词生成一张图片。

> 图像生成落地走你的 ComfyUI 本机沙盒 / AutoDL,本课重点是理解原理 + 会调 diffusers API,
> 生成任何内容前会先给你看目的/预算/参数,等批准再动(你的既定约束)。

**验收点**:能讲清「加噪」和「去噪」两个过程各自在干什么。

---

### 课 7.4 文本/图像到 3D(三维 + 多模态的汇合)

**目标**:把前面学的 3D + 多模态接起来,做「一张图/一句话 → 3D 模型」。

**核心概念**:DreamFusion 用「2D 扩散模型当 3D 先验」,通过 SDS 损失优化 NeRF/3DGS;
Zero123 / InstantMesh / TRELLIS 做图像到 3D。

**命令(AutoDL GPU,用现成开源项目)**:

```bash
# 以 InstantMesh 为例(图像 -> 3D mesh)
git clone https://ghfast.top/https://github.com/TencentARC/InstantMesh.git
cd InstantMesh && pip install -r requirements.txt
python run.py --config configs/instant-mesh-large.yaml --input_path image.png --output_path out/
```

**预期结果**:输入一张物体的单图,输出 `.obj`/`.glb` 3D 模型(可用你的 three.js 查看)。

**验收点**:能解释「2D 扩散模型如何被用来监督 3D 生成」(SDS 损失的思想)。

---

## 10. 毕业项目

**目标**:把「三维重建 + 语义/多模态理解」串成一个端到端 demo,收束整条路线。

**方向(三选一,最后定)**:

- **A · 图片 → 3D 场景 + 语义**:输入几张图,COLMAP 重建点云 + 语义分割上色,导出可交互查看
- **B · 单张图 → 3D 物体 + 文字描述**:InstantMesh 出 mesh,再用 VLM 生成中文描述
- **C · 视频 SLAM 轨迹可视化**:ORB-SLAM 跑一段视频,叠加深度/语义,做出轨迹回放

**验收标准**:能端到端跑通,并能就每个模块讲清「输入/输出/原理」,即视为达到「专家」的第一层(能独立搭系统 + 能读懂论文 + 能复现开源)。

---

## 11. 论文阅读路线(穿插在各阶段)

- 阶段 2:AlexNet / VGG / ResNet(《Deep Residual Learning for Image Recognition》)
- 阶段 3:Faster R-CNN / YOLO / U-Net / Mask R-CNN
- 阶段 4:Multiple View Geometry(教材,选读)
- 阶段 5:NeRF(《NeRF: Representing Scenes as Neural Radiance Fields》)/ 3DGS
- 阶段 6:ORB-SLAM
- 阶段 7:CLIP / DDPM(《Denoising Diffusion Probabilistic Models》)/ DreamFusion

---

## 12. 里程碑验收(分 4 次)

- **里程碑 1(阶段 0~2 完成)**:能独立写出训练循环,CIFAR-10 上 CNN ≥ 75%,迁移学习 ≥ 85%
- **里程碑 2(阶段 3~4 完成)**:能跑通检测/分割推理;能手写相机投影与对极几何计算
- **里程碑 3(阶段 5~6 完成)**:能跑通 COLMAP / NeRF / 3DGS 任一重建;ORB-SLAM 跑通一个序列
- **里程碑 4(阶段 7 完成)**:CLIP 零样本分类 + VLM 问答 + 扩散生成 + 一个图像到 3D demo

---

## 13. 下一步

本方案为 v1,确认后按「阶段 0 课 0.1」开始逐课执行。
每课开课前会先展示该课的具体命令与执行分析,你确认后再动手(延续你 webdev 的协议)。
需要调整阶段数量、增删方向(比如加强检测或砍掉 SLAM)现在提。
